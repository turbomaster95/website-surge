{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "main": ".main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "git": {
    "scripts": {
      "pre-push": "surge --project ./ --domain codegameerr.surge.sh"
    }
  },
  "devDependencies": {
    "git-scripts": "^0.6.4",
    "surge": "^0.23.1"
  }
}
